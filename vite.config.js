import { defineConfig } from 'vite';

export default defineConfig({
    // API-only project - Frontend build removed
    // Add Vite configuration here if you plan to add admin panel or frontend later
});
